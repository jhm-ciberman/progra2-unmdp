7. Obtener el resultado de las siguientes expresiones, previa asignación de valores a las cuatro
variables:

```c
int a = 1, b = 3, c = 10, d = 2;
```


a) ```a + b * c / d```

```
16
```

b) ```(a + b) * c % d```

```
0
```

c) ```d < 65 && 3 == d```

```
0
```

d) ```a <= b || (3 > a)```

```
1
```

e) ```(a >= d) && (2 - 14 == c + 1)```

```
0
```

f) ```a>b || a<c && a = c || a >= b```

```
ERROR
```